import 'package:flutter/material.dart';

class WantedCriminalsView extends StatelessWidget {
  const WantedCriminalsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
