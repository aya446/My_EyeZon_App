import 'package:flutter/material.dart';

class CriminalStatusView extends StatelessWidget {
  const CriminalStatusView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
