import 'package:flutter/material.dart';

class AddAdminCriminalView extends StatelessWidget {
  const AddAdminCriminalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
